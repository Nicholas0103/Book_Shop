.slider-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    margin: 0 auto;
}

.slider-track {
    display: flex;
    overflow-x: auto;
    scroll-behavior: smooth;
    padding: 10px 0;
    scrollbar-width: none;
    -ms-overflow-style: none;  
}

.slider-track::-webkit-scrollbar {
    display: none; 
}

.book-card {
    flex: 0 0 calc(25% - 15px);
    display: flex;
    flex-direction: column;
    align-items: center;
}

.book-cover-link {
    display: block;
    width: 12rem;
    height: 20rem; 
    margin-bottom: 15px;
    border-radius: 12px;
    overflow: hidden; 
    border: 1px solid var(--hover-color); 
    background-color: var(--bg-color);
}

.book-cover:hover {
    transform: scale(1.03);
    opacity: 1.0;
}

.book-cover {
    width: 100%;
    height: 100%;
    opacity: 0.8;
    object-fit: cover; 
    display: block;
    transition: all 0.3s ease;
}

.buy-btn {
    background-color: #FFFDF8;
    color: #5A3500;
    border: none;
    padding: 0.3rem 3rem 0.1rem 3rem;
    border-radius: 20px;
    font-family: Aulieny;
    font-size: 0.95rem;
    cursor: pointer;
    transition: transform 0.2s, background-color 0.2s;
}

.buy-btn:hover {
    background-color: var(--accent-color);
    color: var(--hover-color);
    transform: translateY(-2px);
}

.arrow-btn {
    background-color: #FFFDF8;
    color: #5A3500;
    border: none;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    cursor: pointer;
    font-size: 1.1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    transition: background-color 0.2s;
}

.arrow-btn:hover {
    background-color: #D4AF37;
}

.left-arrow {
    margin-right: 15px;
}

.right-arrow {
    margin-left: 15px;
}

@media (max-width: 768px) {
    .book-card {
        flex: 0 0 calc(50% - 10px); /* 2 cards visible on mobile screens */
    }
}


- C ozy Mystery (Relaxing detective story)
-      Humorous Mystery (Comedic mystery story)
- 1    Supernatural Mystery (with Ghosts, Curses, Unexplained phenomena)
- Classic Detective (Traditional)
- YA & Kid Mystery (For young adults and kids)
- Historical Mystery (Vintage, -1930s)